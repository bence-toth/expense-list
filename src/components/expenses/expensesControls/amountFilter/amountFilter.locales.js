const locales = {
  'en-GB': {
    filterOnExpenseAmount: ({currency}) => `Filter on expense amount (${currency})`
  },
  'en-US': {
    filterOnExpenseAmount: ({currency}) => `Filter on expense amount (${currency})`
  },
  'dk-DK': {
    filterOnExpenseAmount: ({currency}) => `Filter baseret på værdi (${currency})`
  }
}

export default locales
